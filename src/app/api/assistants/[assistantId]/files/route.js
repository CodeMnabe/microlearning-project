import { NextResponse } from "next/server";
import { createOAIFiles } from "@/lib/services/oAi.services";

export async function POST(req, { params }) {
  try {
  } catch (err) {
    return NextResponse();
  }
}
